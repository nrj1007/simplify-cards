# 2026-05-11 Agent Journal

## Current State

The repository is clean before this journal entry. Latest pushed commit before notes:

```text
e5d8bc4 Add American Express credit card data
```

Current card data is split by issuer under `data/cards/`. There is no longer a root `data/cards.json`.

Card count checkpoint:

```text
american-express.json: 6
au-small-finance.json: 1
axis.json: 32
bank-of-baroda.json: 8
federal-bank.json: 1
hdfc.json: 17
hsbc.json: 7
icici.json: 11
idfc.json: 11
sbi.json: 41
total: 135
```

## Important Recent Changes

- Added `popularityScore` to all card records and a reusable scorer at `scripts/update-popularity-scores.js`.
- Split card data from one large file into issuer files under `data/cards/`.
- Updated `lib/cards.ts` to merge all issuer files in memory and sort globally by `popularityScore`.
- Added Vitest with tests for data loading, helper lookups, and recommendation behavior.
- Added Scapia as two active variants:
  - `scapia-federal` in `data/cards/federal-bank.json`
  - `scapia-bobcard` in `data/cards/bank-of-baroda.json`
- Added American Express India high-priority personal cards:
  - Platinum Travel
  - Membership Rewards
  - Gold Card
  - SmartEarn
  - Platinum Reserve
  - Platinum Card

## Validation Commands

Use these after every data change:

```powershell
.\tools\node\node.exe .\.tmp\validate-cards.js
.\tools\node\npm.cmd test
.\tools\node\node.exe .\node_modules\typescript\bin\tsc --noEmit
```

If `scripts/validate-cards.ts` changes, recompile it first:

```powershell
.\tools\node\node.exe .\node_modules\typescript\bin\tsc --module commonjs --target es2020 --esModuleInterop --resolveJsonModule --skipLibCheck --outDir .tmp scripts\validate-cards.ts
```

## User Preferences And Data Rules

- Product is for Indian credit-card users.
- Keep operating cost low; no database yet.
- Official issuer pages are the source of truth.
- Community sources like TechnoFino are only signals for manual review.
- Skip discontinued and legacy cards unless explicitly requested.
- For now, continue building popular card data before implementing reverse indexes.
- Store data in the relevant issuer file under `data/cards/`.
- After adding cards, update popularity scores, validate, test, type-check, commit, and push.

## Recommended Next Work

The next best step is to continue popular issuer data before reverse indexes.

Recommended order:

1. Kotak Mahindra Bank
   - Myntra Kotak Credit Card
   - Kotak League / White / Zen / IOCL / 811 or active popular variants
   - Verify official active-application status first.

2. RBL Bank
   - BookMyShow Play
   - ShopRite
   - World Safari
   - IndianOil
   - BankBazaar SaveMax, if active

3. IndusInd Bank
   - Legend
   - Tiger
   - Pinnacle
   - EazyDiner
   - Avios
   - Platinum RuPay

4. Standard Chartered
   - Ultimate
   - Smart
   - Rewards
   - EaseMyTrip
   - Super Value Titanium

5. YES Bank
   - Marquee
   - Reserv / First Exclusive / First Preferred
   - Paisabazaar co-brands if active

6. Remaining Federal Bank cards
   - Imperio
   - Celesta
   - Signet
   - Wave
   - OneCard variants if active/open

After these popular issuer batches, implement reverse indexes:

- issuer
- tags
- network
- reward category
- popularity

Then move to affiliate-link fields, ad placement components, and the Ask AI retrieval layer.

## Known Notes

- The current Amex entries use official Amex pages, but some old Amex pages can show stale application-availability language. Recheck before making strong availability claims.
- Scapia should remain modeled as two issuer-backed variants because Federal Bank and BOBCARD terms/support can diverge.
- The npm audit check after adding Vitest found no high-severity issues. Remaining low/moderate findings should not be force-fixed casually because npm suggested version jumps.
