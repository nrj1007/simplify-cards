# 2026-05-09 Daily Journal

## Session Summary

Built and expanded the CreditCardAI MVP for Indian credit-card discovery. The project is a lean Next.js-only app using `data/cards.json` loaded in memory. No database is used yet.

User preference: keep operating cost minimal, avoid over-engineering, commit/push card data frequently, and preserve enough context so a future AI session can continue without re-discovery.

## Current Repo

Repository:

```text
https://github.com/nrj1007/creditCardAI
```

Local workspace:

```text
C:\Users\manpr\Documents\Codex\2026-05-08\i-want-to-build-an-ai
```

Branch:

```text
main
```

Portable local tools:

```text
tools/node
tools/git
```

Run the app:

```powershell
cd "C:\Users\manpr\Documents\Codex\2026-05-08\i-want-to-build-an-ai"
.\scripts\dev.ps1
```

Validate:

```powershell
.\tools\node\node.exe .\node_modules\typescript\bin\tsc --module commonjs --target es2020 --esModuleInterop --resolveJsonModule --skipLibCheck --outDir .tmp scripts/validate-cards.ts
.\tools\node\node.exe .\.tmp\validate-cards.js
.\tools\node\node.exe .\node_modules\typescript\bin\tsc --noEmit
```

## Product Direction

MVP architecture:

```text
Next.js App Router
cards.json in memory
deterministic recommendation engine
AI/Q&A as explanation layer
official sources as source of truth
```

Important principle:

```text
Technofino/community posts are early signals only.
Official issuer pages/PDFs must verify any card data update.
```

Monetization thesis:

```text
Ads cover monthly baseline cost.
Affiliate approvals become profit.
```

## Card Database Status

Current validated card count:

```text
42 cards
```

Latest validation result:

```text
Card validation passed for 42 cards with 0 warning(s).
```

Recent SBI additions pushed one card per commit:

- `b269250` PhonePe SBI Card PURPLE
- `7f666b6` PhonePe SBI Card SELECT BLACK
- `311c6b9` IndiGo SBI Card
- `5c82a82` IndiGo SBI Card ELITE
- `77f684f` KrisFlyer SBI Card
- `a4887c9` KrisFlyer SBI Card Apex
- `a6285aa` Landmark Rewards SBI Card
- `aee032d` Landmark Rewards SBI Card SELECT
- `d77f67f` Landmark Rewards SBI Card PRIME

## Mobile Responsiveness Check

Checked the app at a 390px-wide mobile viewport.

Findings:

- Home page stacks cleanly.
- Finder page stacks cleanly.
- Card detail page is readable on mobile.
- Compare page needed a table wrapper for mobile horizontal scrolling.

Local uncommitted responsive fixes currently exist:

- `app/compare/page.tsx`
- `app/globals.css`

These changes passed:

```powershell
.\tools\node\node.exe .\node_modules\typescript\bin\tsc --noEmit
```

Before continuing unrelated work, decide whether to commit these as:

```text
Improve mobile compare layout
```

Update: mobile compare layout fixes were committed and pushed as:

```text
b03a7ce Improve mobile compare layout
```

## Trust Indicator UI

Added UI trust indicators so users and future AI sessions can distinguish official source quality from entries that need review.

Implemented:

- compact verification badges on card tiles
- detail-page verification badge near the title
- `Trust & Source` panel on card detail pages
- status-specific copy for:
  - `official-direct`
  - `official-indexed`
  - `official-catalogue`
  - `official-mixed`
  - `needs-review`

Files:

- `app/ui/VerificationBadge.tsx`
- `app/ui/CardTile.tsx`
- `app/cards/[id]/page.tsx`
- `app/globals.css`

Validation:

```text
TypeScript passed.
Browser check confirmed badges render on home/card detail pages.
Mobile viewport check at 390px showed the detail badge fits cleanly.
```

## Community Signals Skill

Created a repo-contained Codex skill for manual community-signal collection:

```text
skills/community-signals/
```

Purpose:

- scrape public Technofino new-post pages
- extract credit-card related threads and recent comments
- write JSON into `data/community-signals/pending/`
- require manual approval before any official card data change

Important rule:

```text
Never update data/cards.json from community signals alone.
```

Manual run command:

```powershell
.\tools\node\node.exe .\skills\community-signals\scripts\scrape-technofino.mjs --feed "https://technofino.in/community/whats-new/posts/5982841/" --pages 5 --hours 24
```

Dry-run validation completed:

- skill files present
- `SKILL.md` frontmatter present
- `agents/openai.yaml` present
- scraper syntax passed `node --check`
- small network test wrote `.tmp/community-signals-test-10.json`
- TypeScript still passed

## Technofino Scrape Notes

User asked to scrape:

```text
https://technofino.in/community/whats-new/posts/5982841/
```

Important behavior:

- This URL returns the public Technofino "New posts" feed, not a single post.
- It has pagination such as `/page-2`.
- Direct workspace fetch needed network approval.
- Public feed fields extracted successfully: title, URL, author, forum, replies, views, latest activity.

Last scrape focus: public posts/comments from approximately the last 24 hours, filtered for credit-card relevance.

High-signal items:

1. IDFC FIRST Wealth Credit Card update
   - Official PDF found and should be treated as the real source.
   - Effective date: 2026-06-18.
   - 10X rewards on Dining, Travel, and International from first eligible transaction.
   - Earlier Rs 20,000 billing-cycle unlock for 10X categories removed.
   - Reward points valid for 24 months from July 2026 statement cycle.
   - Existing points as of July 2026 valid until July 2028.
   - BOGO movie benefit will require Rs 20,000 previous-calendar-month spend from 2026-08-01.
   - Official source:
     `https://www.idfcfirst.bank.in/content/dam/idfcfirstbank/pdf/credit-card/FIRST-Wealth-Credit-Card-Communication-May-26.pdf`
   - Technofino thread:
     `https://technofino.in/community/threads/devaluation-alert-for-idfc-first-bank-credit-card.48154/`

2. Axis Airtel Credit Card devaluation discussion
   - Users report April 2026 cashback credits.
   - Several users discussed or reported closing cards.
   - Some report no retention offers.
   - Needs Axis official T&C verification before any data update.
   - Thread:
     `https://technofino.in/community/threads/axis-airtel-devaluation-w-e-f-12-april-2026.47422/`

3. Kotak Cashback+ RuPay LTF signal
   - A user reported receiving Kotak Cashback+ RuPay as LTF against Kotak Super 811 Savings Account.
   - Community reaction was skeptical.
   - Treat as acquisition/offer signal only.
   - Thread:
     `https://technofino.in/community/threads/kotak-cashback-rupay-cc-ltf-is-it-of-any-use.48164/`

4. SBI Cashback / PhonePe merchant data
   - Ongoing community tracking of merchant names and reward behavior.
   - Recent mention: Amazon merchant showing as `ASSPL`.
   - Useful for future merchant-data module, not direct card JSON source.
   - Threads:
     `https://technofino.in/community/threads/getting-asspl-merchant-name-for-regular-amazon-purchases-with-sbi-cashback-card.45920/`
     `https://technofino.in/community/threads/sbi-cashback-card-phonepe-purple-black-for-all-merchant-wise-data-collection.27562/`

5. Federal Bank / Standard Chartered India card takeover discussion
   - Community discussing Federal Bank taking over Standard Chartered India credit cards.
   - Comments are mostly speculation/user expectations.
   - Needs official bank/regulatory source before any product update.
   - Thread:
     `https://technofino.in/community/threads/federal-bank-to-take-over-standard-chartered-india-credit-cards.48085/`

6. BOB Eterna LTF and reward behavior
   - Active discussion around accelerated rewards and gift-card/offline spend behavior.
   - Useful for FAQ/caveats only until verified.
   - Thread:
     `https://technofino.in/community/threads/ltf-bob-eterna-credit-card.44751/`

## Recommended Next Work

1. Commit the mobile compare layout fixes if the user agrees or if continuing UI polish.
2. Add IDFC FIRST Wealth Credit Card from official PDF/source.
3. Build a `community-signals` scraper module later:
   - input: Technofino feed URL
   - output: normalized signal JSON
   - fields: source, thread, title, forum, latest timestamp, signal type, needs official verification
4. Add a review workflow:
   - community signal
   - official verification
   - card JSON update
   - validation
   - one-card commit and push

## Do Not Forget

- Do not treat community posts as official data.
- Keep one card per commit.
- Run card validation and TypeScript before committing card updates.
- Preserve unrelated user/local changes.
- Use official issuer pages/PDFs for card terms.
