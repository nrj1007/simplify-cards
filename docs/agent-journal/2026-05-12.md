# 2026-05-12

Fallback local save because the dedicated journal repo `nrj1007/journal` was not available in this workspace/session.

## What Changed

- Added optional `Latest Updates` and `Tips` sections on card detail pages, backed by `data/card-content.json`.
- Added a TechnoFino community-signal review and ingestion path:
  - review page: `app/review/community/page.tsx`
  - helpers: `lib/community-signals.ts`
  - ingest script: `npm run ingest:technofino-content`
  - scraper now includes `publishedAt` in pending signal JSON
- Added a `Signals` nav link to the review surface.
- Added tests for community-signal drafting, matching, and merge behavior.

## Important Product Rule Confirmed

- The user explicitly clarified that **nothing from the scraper should go into the database automatically**.
- Required flow:
  1. scraper writes pending JSON under `data/community-signals/pending/`
  2. user reviews manually
  3. only explicitly approved items may be ingested into `data/card-content.json`
- This applies even to TechnoFino-derived `Latest Updates` and `Tips`.
- Core issuer card data under `data/cards/` remains stricter still and should never be updated from community signals alone.

## Current Status

- Main repo latest commit after this work: `5c5838e Add Technofino content ingestion review flow`
- Type check passed
- Test suite passed: `40` tests
- Working tree was clean after push

## Next Recommended Step

- Tighten the TechnoFino review flow so the UI and/or ingest script make the manual-approval requirement even more explicit, reducing future ambiguity.
