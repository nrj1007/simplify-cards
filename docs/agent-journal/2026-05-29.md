# 2026-05-29

## TODO

- Build reverse indexes for issuer, tags, network, reward category, and popularity.
- Build deterministic retrieval helpers on top of those indexes.
- Ground `Ask AI` answers in retrieved card records plus source links, with no web search fallback.
- Log unsupported or stale questions for manual database updates by the user.
- Add affiliate-link fields and ad-slot plumbing after retrieval is in place.
